# 人机协作实验语境

这个独立仓库用于验证 HY-Motion 生成人体动作在 Genesis 中的可视化、空间回放和人机交互实验接口。它不拥有 `gaze_vr_isaaclab` 的机器人业务代码，只通过明确的场景和任务契约与机器人实验对接。

## 参与者与表示

**人体视觉层**：由官方 HY-Motion WoodenMesh 生成的完整三角网格，只负责显示，不参与动力学碰撞。

**人体碰撞代理**：由 22 关节 MJCF 球关节、胶囊和球体组成的低复杂度人体表示，负责近距离安全检查和未来接触建模。

**机器人**：Genesis 中加载的 Galaxea R1 Pro 移动机械臂，由独立机器人项目提供，不由本仓库修改或拥有。

**动作片段**：一段带 FPS 和 SMPL-H `poses/trans` 的 HY-Motion 输出；当前基准片段的语义是“向前行走、转向侧面、单手向前伸出并停顿”。

## 任务语言

**交互窗口**：人体动作中，指定手腕相对骨盆速度低于阈值并保持足够时长的时间区间。当前基准片段使用左手腕，预计在末段形成窗口。

**接近阶段**：机器人从初始位置移动到人体交互区外侧的准备位，同时满足最小安全距离。

**交互准备位**：机器人末端到达左手腕目标附近、但不与人体碰撞代理接触的位置。

**预抓取位**：机器人末端在物体外侧、尚未接触物体的安全姿态。预抓取位与最终抓取位分开，机器人必须先到达预抓取位再低速接近。

**交接物体**：由人体在交接前控制、由机器人在抓取验证后控制的刚体。交接物体不是直接瞬移到夹爪上的视觉标记。

当前阶段一的运动学基线使用 WoodenMesh 左手关键点构造掌心局部坐标系，将物体放在
拇指根部和食指根部之间并向掌心内部小幅偏移；物体尺寸约为 `0.05 m`，不再使用
左腕沿前臂方向的单一延长线。

**抓取验证**：夹爪闭合后，依据接触和物体相对夹爪位姿稳定性确认机器人确实持有物体。

**控制权转移**：交接物体从人体手部运动学跟随切换为机器人夹爪约束或动力学控制的事件。

**个人空间约束**：机器人碰撞几何与人体碰撞代理之间的最小距离下限。它不是网格表面距离，也不是允许机器人穿过人体后的修正量。

**任务成功**：人体完成动作、机器人以受限速度安全到达预抓取位和抓取位，成功取得交接物体并撤回，且全过程没有碰撞、明显传送或安全距离违例。

**失败**：动作数据无法播放、机器人无法到达准备位、交互窗口错过、发生碰撞、违反个人空间，或任一实体出现非预期的位姿跳变。
